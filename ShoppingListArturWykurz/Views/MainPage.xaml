<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ShoppingListArturWykurz.Views"
    x:Class="ShoppingListArturWykurz.Views.MainPage"
    Title="Lista zakupów">

    <Grid RowDefinitions="Auto,*,Auto" Padding="0" RowSpacing="0" BackgroundColor="#D3F3EE">

        <!-- Header z nazwą -->
        <BoxView Grid.Row="0" BackgroundColor="#7FB7BE" HeightRequest="60" />
        
        <Label
            Grid.Row="0"
            Text="LISTA ZAKUPÓW"
            FontSize="24"
            FontAttributes="Bold"
            TextColor="White"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Margin="0,10" />

        <!-- scroll view dla kategorii -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout 
                x:Name="CategoriesContainer"
                Padding="10"
                Spacing="10" />
        </ScrollView>

        <!-- Footer z przyciskami -->
        <StackLayout 
            Grid.Row="2"
            Orientation="Horizontal"
            Padding="10"
            Spacing="5"
            BackgroundColor="#7FB7BE">

            <!-- Przycisk do dodania kategorii -->
            <Button 
                x:Name="AddCategoryButton"
                Text="➕ Kategoria"
                WidthRequest="150"
                HeightRequest="50"
                BackgroundColor="#7D1538"
                TextColor="White"
                CornerRadius="10"
                Clicked="OnAddCategoryClicked"
                FontSize="14" />

            <!-- Przycisk do eksportowania listy -->
            <Button 
                x:Name="ExportButton"
                Text="📤 Eksportuj"
                WidthRequest="150"
                HeightRequest="50"
                BackgroundColor="#BC2C1A"
                TextColor="White"
                CornerRadius="10"
                Clicked="OnExportClicked"
                FontSize="14" />

            <!-- Przycisk do importowania listy -->
            <Button 
                x:Name="ImportButton"
                Text="📥 Importuj"
                WidthRequest="150"
                HeightRequest="50"
                BackgroundColor="#DACC3E"
                TextColor="White"
                CornerRadius="10"
                Clicked="OnImportClicked"
                FontSize="14" />
            
        </StackLayout>
        
    </Grid>
    
</ContentPage>